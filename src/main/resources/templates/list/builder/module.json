{
    "widgets": [
        {
            "id": "{{ARG1}}",
            "title": "{{ARG1}}",
            "settings": {
                "viewUrl": "index.html",
                "example_setting": "some value"
            }
        }
    ]
}